apiVersion: v1
kind: Service
metadata:
  name: {{ include "bhadwa-app.fullname" . }}
  labels:
    {{- include "bhadwa-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "bhadwa-app.selectorLabels" . | nindent 4 }}
